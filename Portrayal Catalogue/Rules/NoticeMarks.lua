-- LUA script
-- Notice Marks main entry point.


 require 'NoticeMark'

function NoticeMarks(feature, featurePortrayal, contextParameters)
	local viewingGroup

	for mark in feature.NoticeMark
        -- use position and number of marks to calculate the position in the circle

        -- call the script 'NoticeMark' to display the mark
    end
	return viewingGroup
end
